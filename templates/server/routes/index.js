'use strict';

/**
 * <%= serviceName %> routes
 */

const Demo = require( './demo' );

let ROUTES = [];

ROUTES = ROUTES
        .concat( Demo );

/**
 * Exports: ROUTES
 */

module.exports = ROUTES;
